


<li>

	<div class="card mt1" id = "burger-card">
	<div class="card-body">
		<div class="card-title h5">{{burger_name}}</div>
		<a href="#" class="btn btn-primary change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}"> {{#if devoured}}MENU{{else}}EAT{{/if}} </a>
	</div>
	</div>

</li>